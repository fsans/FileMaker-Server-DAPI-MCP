# FileMaker Data API MCP Server - Transport Configuration

# Transport type: stdio (default), http, or https
# - stdio: Local stdio communication (default, for Claude Desktop)
# - http: HTTP server on network
# - https: HTTPS server on network (requires certificates)
MCP_TRANSPORT=stdio

# HTTP/HTTPS Server Configuration
# Port to listen on (default: 3000 for HTTP, 3443 for HTTPS)
MCP_PORT=3000

# Host to bind to (default: localhost)
# Use 0.0.0.0 to listen on all interfaces
MCP_HOST=localhost

# HTTPS Certificate Configuration (required only for HTTPS transport)
# Path to SSL certificate file
MCP_CERT_PATH=/path/to/cert.pem

# Path to SSL private key file
MCP_KEY_PATH=/path/to/key.pem

# FileMaker Server Configuration
FM_SERVER=your-filemaker-server.com
FM_VERSION=vLatest
FM_DATABASE=your_database
FM_USER=your_username
FM_PASSWORD=your_password

# External Databases (optional)
# JSON array of external database credentials
# FM_EXTERNAL_DATABASES=[{"database":"external_db","username":"user","password":"pass"}]
